import React from 'react';
import {View} from 'react-native';
import {PADDING_LEFT} from '../../styles/spacing';

const Padding = () => <View style={{paddingTop: PADDING_LEFT}} />;

export default Padding;
