import React from 'react';
import { View } from 'react-native';
import { PADDING_LEFT } from '../../../styles/spacing';

const HalfPadding = () => <View style={{ paddingTop: PADDING_LEFT / 2 }} />;

export default HalfPadding;
